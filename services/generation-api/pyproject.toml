[project]
name = "synaptix-generation-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["fastapi>=0.116", "uvicorn[standard]>=0.35", "pydantic>=2.11"]
[project.optional-dependencies]
dev = ["pytest>=8.4", "httpx>=0.28"]
